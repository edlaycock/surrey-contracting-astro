import { defineCliConfig } from 'sanity/cli';

export default defineCliConfig({
  api: { projectId: 'mhqgpyb9', dataset: 'production' },
  // Hosted Studio URL: https://surreycontracting.sanity.studio
  studioHost: 'surreycontracting',
});
